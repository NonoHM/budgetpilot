{
	"attribution": {
		"sessionUrl": false
	},
	"permissions": {
		"deny": [
			"Bash(npx prisma migrate reset*)",
			"Bash(prisma migrate reset*)",
			"Bash(docker compose down -v*)",
			"Bash(docker-compose down -v*)",
			"Skill(superpowers:finishing-a-development-branch)",
			"Skill(superpowers:using-git-worktrees)"
		],
		"ask": ["Bash(npx prisma migrate*)", "Bash(docker compose down*)"]
	},
	"hooks": {
		"SessionStart": [
			{
				"hooks": [
					{
						"type": "command",
						"command": "git diff --stat HEAD",
						"statusMessage": "Checking the working tree"
					}
				]
			}
		],
		"PreToolUse": [
			{
				"matcher": "Bash",
				"hooks": [
					{
						"type": "command",
						"command": "$CLAUDE_PROJECT_DIR/.claude/hooks/git-remote-current.sh",
						"statusMessage": "Checking the branch point against origin"
					},
					{
						"type": "command",
						"command": "node \"$CLAUDE_PROJECT_DIR/.claude/hooks/private-references.mjs\" || exit 2",
						"timeout": 10,
						"statusMessage": "Checking for private references before publishing"
					}
				]
			},
			{
				"matcher": "mcp__.*github.*",
				"hooks": [
					{
						"type": "command",
						"command": "node \"$CLAUDE_PROJECT_DIR/.claude/hooks/private-references.mjs\" || exit 2",
						"timeout": 10,
						"statusMessage": "Checking for private references before publishing"
					}
				]
			}
		]
	}
}
